# Auth Testing Playbook
Generated by integration_playbook_expert_v2 for this project.

## Admin credentials (from /app/backend/.env)
- Email: operations@panoramacoaches.com.au
- Password: Pano3666

## Step 1 — MongoDB verification (inside container)
```
mongosh
use test_database
db.users.find({role: "admin"}, {password_hash: 0}).pretty()
db.users.findOne({role: "admin"}, {password_hash: 1})   // should start with $2b$
db.users.getIndexes()                                   // email unique
db.login_attempts.getIndexes()
db.password_reset_tokens.getIndexes()                   // TTL on expires_at
```

## Step 2 — API checks
```
BASE=http://localhost:8001
curl -c cookies.txt -X POST $BASE/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"operations@panoramacoaches.com.au","password":"Pano3666"}'
cat cookies.txt
curl -b cookies.txt $BASE/api/auth/me

# Create a user (admin only)
curl -b cookies.txt -X POST $BASE/api/users \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"test1234","name":"Test User","role":"user"}'

# Post a message
CH=$(curl -b cookies.txt -s $BASE/api/channels | python3 -c "import sys,json;print(json.load(sys.stdin)[0]['id'])")
curl -b cookies.txt -X POST $BASE/api/messages/channel/$CH \
  -H "Content-Type: application/json" \
  -d '{"content":"Hello, team!","attachments":[]}'
```

## Step 3 — Brute force check
Run 6 login attempts with wrong password; on the 6th, expect 429.

## Step 4 — WebSocket check
Connect to `wss://<host>/api/ws` with `?token=<access_token>` and send
`{"type":"subscribe","channel_id":"<ID>"}`. Posting a message via REST should push a broadcast.
