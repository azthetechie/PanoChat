# Panorama Comms — backend runtime requirements.
# Minimal top-level deps. Transitive dependencies (cryptography, http-ece,
# pydantic-core, starlette, anyio, sniffio, click, dnspython, ecdsa, etc.)
# are resolved automatically by pip — no need to pin them here.
#
# Install:
#   pip install -r requirements.txt

# --- Web framework ---
fastapi==0.110.1
uvicorn[standard]==0.25.0
python-multipart==0.0.24          # required for UploadFile / multipart forms

# --- Database ---
motor==3.3.1                       # async MongoDB driver

# --- Auth & crypto ---
PyJWT==2.12.1
bcrypt==4.1.3
python-dotenv==1.2.1

# --- Web-push (PWA notifications) ---
pywebpush==2.3.0
py-vapid==1.9.4

# --- Email ---
resend==2.29.0
email-validator==2.3.0

# --- QR codes (admin onboarding poster) ---
qrcode[pil]==8.2

# --- Data models ---
pydantic==2.12.5

# --- Test-only (kept in prod for /tests/ — remove to slim down further) ---
pytest==9.0.3
requests==2.32.5
websockets==15.0.1
httpx==0.28.1
